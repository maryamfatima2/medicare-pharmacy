{
  "name": "healora-pharmacy-root",
  "version": "1.0.0",
  "description": "Healora Pharmacy Full Stack",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npx concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "eslint": "^8.57.1"
  }
}
